import { describe, it } from "bun:test";
import { PlotPoints } from "../../src/db/services";
import * as NARRATIVE from "../fixtures/narrative";

describe("toChain", () => {
  it("should do stuff", () => {
    // console.dir(PlotPoints.toChain(NARRATIVE.ONE), { depth: null });
  });
});
