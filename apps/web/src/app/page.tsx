import { StandardLayout, Theme } from "@2pm/ui";

export default async function Home() {
  return (
    <Theme themeId="frappe">
      <StandardLayout.Root>{null}</StandardLayout.Root>
    </Theme>
  );
}
